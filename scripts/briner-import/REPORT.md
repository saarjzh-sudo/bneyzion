# ייבוא תכני הרב יוסי ברינר ("ושננתם") מ-otzar.org.il — דוח

**תאריך:** 19.7.2026, סשן רקע (אושר ע"י סער + מיפוי מחייב של הרב יואב)
**DB:** Supabase `pzvmwfexeiruelwiujxn` (בני ציון)

---

## 1. מצב קיים (לפני)

שלוש ישויות "הרב יוסי ברינר" בטבלת `rabbis`:

| id | slug | entity_type | שיעורים | סדרות |
|----|------|-------------|---------|--------|
| `6f4b2572-b019-4832-9547-de7e8bc6d909` | `veshinantam-otzar-hatorah` | content_creator | 1,295 | 44 |
| `e6c81665-5737-4c82-b96e-7527cc0e6ee6` | `veshinantam` | content_creator | 58 | 5 |
| `d68b38ad-fd70-47d3-b667-20f68af6d8c4` | `yvsy-brynr` | rabbi | 34 | 0 |

רוב הסדרות הקיימות מתויגות `["teachers"]` בלבד. סמנטיקת האפליקציה (DesignPreviewSeriesPageV2 §0.3):
תיוג כפול `['general','teachers']` = מופיע בשני האגפים; teachers-בלבד = redirect לאגף המורים.

## 2. סריקת otzar.org.il

- קטגוריה 1 — **אוצר מפרשי התנ"ך**: 40 פריטים (4 עמודי listing).
- קטגוריה 2 — **אוצר המקרא על התנ"ך**: 690 פריטים (69 עמודי listing).
- **סה"כ 730 מאמרים, 0 שגיאות fetch.** קצב ~1.4 בקשות/שנייה.
- לכל מאמר נשמרו: כותרת, breadcrumb (הקטגוריה המקורית), תוכן HTML מלא (`lessonContent`),
  מזהה קצר של האתר (otzar_id), מחבר, תקציר.
- קבצים: `otzar_catalog.json` (המיפוי), `articles.jsonl` (התוכן המלא), `scrape_log.jsonl`.
- הערת מקור: תת-קטגוריה "הושע/פרקים י-ט" — כנראה typo באתר המקור (נשמר כמו שהוא, לפי
  "לפי הקטגוריות המקוריות שלו").

## 3. תוכנית סיווג (`import_plan.json`)

כללי הרב יואב (מילוליים):

- כותרת מכילה שאלות / ביאורי מילים / מספרים / מקומות / דפי עבודה / מי אמר למי → `["teachers"]`.
- כותרת מכילה "ושננתם" → אגף רגיל.
- אחרת (פריט מיובא חדש) → `["general"]`.

תוצאה: **728 general / 2 teachers**. שני הפריטים שסווגו למורים הם התאמות מילוליות:
"מספרים עגולים במקרא" (בגלל "מספרים") ו"שאלות שונות" — ⚠️ מומלץ שהרב יואב יאשר,
שניהם נראים כמאמרים כלליים.

מבנה סדרות חדש (113 סדרות, כולן `draft`), משקף 1:1 את עץ הקטגוריות של האתר המקורי:

- אוצר מפרשי התנ"ך → מאמרים כלליים / תורה (בראשית…דברים) / נביאים / כתובים
- אוצר המקרא על התנ"ך → על התורה / לספרי הנביאים / לספרי הכתובים → ספר → טווח-פרקים

**4 היפותזות-כפילות** (כותרת זהה לשיעור ברינר קיים) — לא יובאו, ממתינות להכרעה:

1. תורה - נביאים - כתובים (`/מאמרים-כלליים/תורה-נביאים-כתובים/`)
2. עבודת הבכורות בבית המקדש (`/תורה/במדבר/עבודת-הבכורות-בבית-המקדש/`)
3. נזירות שמשון ושמואל (`/תורה/במדבר/נזירות-שמשון-ושמואל/`)
4. אנשי כנסת הגדולה (`/כתובים/עזרא/אנשי-כנסת-הגדולה/`)

## 4. גיבוי

נוצרו לפני כל כתיבה ואומתו מול הספירות החיות:
`lessons_bak_briner_20260719` (23,347) · `series_bak_briner_20260719` (1,751) · `rabbis_bak_briner_20260719` (214).

## 5. פיילוט (2 פריטים, מקצה לקצה) — ✅ עבר

| פריט | אגף | סטטוס | תוכן | שרשרת סדרות |
|------|-----|--------|-------|--------------|
| נחלות בני יוסף | general | draft | 16,643 תווים HTML | בראשית ← תורה ← אוצר מפרשי התנ"ך |
| מספרים עגולים במקרא | teachers | draft | 30,882 תווים HTML | מאמרים כלליים ← אוצר מפרשי התנ"ך |

בדיקת URL ציבורי: לא רלוונטית בשלב זה — כל הייבוא ב-`status='draft'`, וכל המסכים הציבוריים
מסננים `status='published'` (אומת בקוד: `useLesson.ts`, `useLessonsBySeries.ts`). שום דבר
לא נחשף לגולשים עד אישור פרסום.

## 6. ייבוא מלא — ✅ הושלם 19.7.2026 ~22:20

- **726 שיעורים חדשים** הוכנסו (מתוך 730 שנסרקו; 4 דולגו כחשד-כפילות — סעיף 3).
- **113 סדרות חדשות** — עץ הקטגוריות המקורי במלואו.
- הכל בסטטוס **`draft`** — לא גלוי לגולשים עד אישור פרסום.
- rabbi_id לכולם: `6f4b2572-b019-4832-9547-de7e8bc6d909` (הישות המרכזית של ברינר).
- מקור לכל שיעור נשמר ב-`additional_attachments->source` (site/url/otzar_id) — מפתח אידמפוטנטיות;
  id דטרמיניסטי (uuid5 מה-URL), הרצה חוזרת = ON CONFLICT DO NOTHING.
- **תיקון המשך:** 22 שיעורים היו עמודי שו"ת במבנה שונה (questionBlock/answerBlock) ונקלטו ריקים —
  `fix_qa_content.py` השלים את כולם (fixed=22, failed=0). אימות: 0 שיעורים ריקים,
  אורך תוכן מינימלי 126 תווים, ממוצע ~31,600.

## 7. סיווג-מחדש של תוכן קיים ("ושננתם" → +general) — ✅ בוצע

עדכון מוסיף בלבד (`audience_tags || ['general']`), שום תיוג לא הוסר.

**15 סדרות** (כולן היו `["teachers"]`, עכשיו `["teachers","general"]`):
`ee16bb53-a2cf-49a7-bba7-62294dfa6e3d` (שופטים) · `a64ada4b-87b7-46a6-873a-0cdc503ddb34` (שמות) ·
`4915aec2-2343-46d1-b350-f16add13979a` (מגילת אסתר) · `2d121967-7773-4e54-a486-67ecb2fedc00` (מלכים ב) ·
`ad002f20-89a3-476e-8c37-92b285a89b9f` (ירמיהו) · `ca117e7c-b8e4-4c6c-bb78-882ba3a1f591` (שמואל ב) ·
`6762cff2-4323-4382-9116-5d1ee1533f33` (במדבר) · `29eb7c0b-308a-494d-a36f-1fcfdd2cff6a` (ויקרא) ·
`3af005f9-d1b0-4dfa-bc0b-60b92c1e9dd0` (דברים) · `4266e310-fc38-426c-98f3-1f3ebaebdce9` (שמואל א) ·
`dff2b236-73db-4ca0-83de-6f77fe20d2c1` (מלכים א) · `43c1040b-fdda-4dca-a766-428530273b7e` (מגילת איכה) ·
`3f9978c4-c405-4cd0-b95e-ee7e248a69f8` (עזרא) · `896aafa4-82fc-4a0b-93d7-417fecf6d692` (בראשית) ·
`62e24b0d-c0f2-4aef-b305-5f302ccc5b97` (דניאל)

**35 שיעורים** — הרשימה המלאה ב-`progress.jsonl` (רשומת `retag_done`, שדה `lessons_changed`).

הוחרגו (כלל מילות-מורים גובר): 4 שיעורי "דפי עבודה ספר X - ושננתם" — נשארו teachers-בלבד.

## 8. אימות סופי — ✅

| בדיקה | לפני | אחרי | דלתא |
|--------|-------|-------|-------|
| lessons | 23,347 | 24,073 | **+726** (בדיוק המתוכנן) |
| series | 1,751 | 1,864 | **+113** (בדיוק המתוכנן) |
| rabbis | 214 | 214 | 0 (לא נגענו) |

- כל 726 החדשים: `status='draft'`, מקור otzar.org.il, התפלגות `{general}`=724 / `{teachers}`=2.
- כל 113 הסדרות החדשות: `status='draft'`.
- כפילויות כותרת בין ישן↔חדש אצל ברינר: **0**. כפילויות בתוך הייבוא: **0**.
- שיעורים מיובאים ריקים: **0** (אחרי תיקון השו"ת).
- שאריות retag: 0 סדרות, 4 שיעורים (ההחרגות המכוונות של "דפי עבודה").

## 9. פתוח להחלטה (סער / הרב יואב)

1. **פרסום**: הכל draft. פרסום = `UPDATE ... SET status='published'` לסדרות + לשיעורים — באישור בלבד.
2. **2 פריטים שסווגו למורים על התאמה מילולית**: "מספרים עגולים במקרא", "שאלות שונות" — נראים כמאמרים כלליים; לאשר סיווג מול הרב יואב.
3. **4 חשדות-כפילות** שלא יובאו (סעיף 3) — להכריע אם התוכן באתר-המקור זהה לקיים או גרסה עדכנית.
4. **פריטים קיימים עם מילות-מורים שכבר מתויגים general** (כמו "מקומות בספר מלכים א") — לפי הכלל היו צריכים
   להיות teachers-בלבד; הסרה = פעולה גורעת, לא בוצעה.
5. סדרת-האב שבה יופיע התוכן החדש בפועל בניווט האתר (העץ החדש הוא top-level ללא הורה) — לבדוק אחרי הפרסום.

## קבצים

`scripts/briner-import/` — `scrape_otzar.py`, `build_plan.py`, `apply_import.py`, `fix_qa_content.py`,
`otzar_catalog.json`, `articles.jsonl`, `import_plan.json`, `progress.jsonl`, `qa_fixes.jsonl`, `scrape_log.jsonl`.
גיבויים: `lessons_bak_briner_20260719`, `series_bak_briner_20260719`, `rabbis_bak_briner_20260719`.
